using UnityEngine;

public class ItemScript : MonoBehaviour
{
    public bool size;
    // true If Big False If Small
    public bool isScrap;
    // false If It isn't Scrap Item
    public int id;
    // item ID
}
